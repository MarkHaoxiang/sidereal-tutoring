# Local development only. The credentials in .env are plaintext dev placeholders and must
# not be reused anywhere that is reachable from outside this machine.
#
# Postgres uses pgvector/pgvector:pg17 so `CREATE EXTENSION vector` works out of the box;
# Directus itself does not need it, later retrieval over `documents` does.
#
# See directus/README.md for bringing the stack up, applying the schema and minting the
# agent token.
services:
  postgres:
    image: pgvector/pgvector:pg17
    env_file:
      - path: .env
        required: true
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sidereal} -d ${POSTGRES_DB:-sidereal}"]
      interval: 5s
      timeout: 5s
      retries: 10

  directus:
    image: directus/directus:12.3.1
    env_file:
      - path: .env
        required: true
    environment:
      EXTENSIONS_AUTO_RELOAD: "true"
      # From the invoking shell, never from .env — see docs/development.md. Empty means the
      # Core tier, which is what CI runs on.
      LICENSE_KEY: ${LICENSE_KEY:-}
    ports:
      - "8055:8055"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - directus-uploads:/directus/uploads
      # Read-only: the container never writes here. scripts/directus-schema-snapshot.sh has
      # the CLI write inside the container and streams the result out.
      - ./directus/schema:/directus/schema:ro
      - ./directus/extensions:/directus/extensions
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8055/server/ping"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

volumes:
  postgres-data:
  directus-uploads:
