# roles

Empty by design. Schema snapshots carry collections, fields and relations only — not roles,
policies or permissions — so the `Tutor` role, its policy and the `agent@sidereal.example.com`
service account are created by `scripts/directus-bootstrap.sh` instead.
